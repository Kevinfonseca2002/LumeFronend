import { Component } from '@angular/core';
import { EventCard } from '../../../layout/event-card/event-card';

@Component({
  selector: 'app-events',
  imports: [EventCard],
  templateUrl: './events.html',
  styleUrl: './events.scss',
})
export class Events {

}
